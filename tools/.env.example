# Server
NODE_ENV=development # "development" or "production"
LOG_LEVEL=INFO # ERROR, WARN, INFO, DEBUG
PORT=3000

# Storage
USE_MONGO=false # "true" to use MongoDB, "false" to use file storage
MONGO_URL=mongodb://mongo:27017/passkeys-tools # required when USE_MONGO=true
# DATA_FILE=data.json # optional: defaults to "data.json" (dev) or "/data/data.json" (Docker)
