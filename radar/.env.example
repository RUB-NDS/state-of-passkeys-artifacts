# Admin credentials for /admin dashboard
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# Data directory (absolute path recommended for Docker)
# DATA_DIR=/data # defaults to "../data" (local) or "/data" (Docker)
